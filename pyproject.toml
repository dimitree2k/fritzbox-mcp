[project]
name = "fritzbox-mcp"
version = "0.1.0"
description = "MCP server for Fritz!Box router management via Claude Code"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "mcp[cli]>=2.0,<3",
    "fritzconnection>=1.14.0",
    "requests>=2.28.0",
    "python-dotenv>=1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
